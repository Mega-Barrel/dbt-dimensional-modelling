
version: 2

models:
  - name: dim_date
    columns:
      - name: date_key
        description: The surrogate key of the data table.
        tests:
          - unique
          - not_null

      - name: date_day
        description: The natural key of the data table
        tests:
          - not_null
          - unique
